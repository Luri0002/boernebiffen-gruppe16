---
// Henter parametrene fra Astro.props - default værdier er angivet
const { htmlheading="h2", size="medium", font="display", stylingclasses=""} = Astro.props; 
const Htmlheading = htmlheading;
 
//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige størrelser.
const sizeClasses = {
"large": "text-3xl lg:text-6xl",
"medium": "text-2xl lg:text-3xl",
"small": "text-xl",
};

//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige skrifttyper.
const fontClasses = {
"display": "font-display",
};
---
<Htmlheading class:list={[stylingclasses, sizeClasses[size], fontClasses[font]]}><slot /></Htmlheading>